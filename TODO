Problems :
    1. flutter_downloader chỉ lưu đc vào directory có sãn , nêú không có thì sẽ báo lỗi
    2. custom tên file thì phải đi kèm với extension , không là khi tải xuống file không đọc đc ,
    phải chỉnh tay thủ công mới đọc được
    


todo:
    1. connect with worker isolate + take process + pass to main isolate , show progress ✅
    
    2. logic with hive (download_task_custom)
    3. logic with hive(download_task_custom) and flutter_downloader (download_task)
    4.

    5. create folder if not exists 
    6. remove cloud file download option ✅
    7. notification title and description when torrent download 
    8. confirming when cancel foreground service
    9. translate notification
    10.remove speed limit option
    11.remove internet setting option
    12. notification when download completed
    13. languages
    14. sorting download list
    15.remove other parameters in download option (only keep folder path and file name)
    16. fix icons
    17. fix ui
    18. history page
    19 . empty state ui ✅
